apiVersion: v1
kind: ConfigMap
metadata:
  name: activescale-config
  namespace: ns-observability
data:
  # Activescale always ingests inbound-scoped downstream_rq_active metrics.
  # Redis key prefix/context, e.g. "activescale:tcn".
  REDIS_CONTEXT: "activescale:tcn"
  # Redis/Valkey address (<host>:<port>).
  REDIS_ADDR: "redis.custom-metrics.svc.cluster.local:6379"
  # Enable TLS for Redis/Valkey (true/false).
  REDIS_TLS: "true"
  # Redis connection mode (cluster redirects support).
  # Must be explicitly set to avoid accidental misconfiguration.
  REDIS_CLUSTER: "false"
  # Envoy gRPC sink listen port.
  GRPC_PORT: "9000"
  # Default 32 MiB max gRPC receive message size for Envoy StreamMetrics batches.
  GRPC_MAX_RECV_MSG_SIZE: "33554432"
  # Metric TTL duration.
  METRIC_TTL: "20s"
  # Klog verbosity: 0=info, 1-3=more detail, 4=debug (e.g., per-pod values).
  LOG_VERBOSITY: "0"
  # Summary logging interval for Envoy ingest and API responses.
  LOG_METRICS_SUMMARY_INTERVAL: "30s"
